# Runtime (Slicer provides numpy/vtk at runtime; list extras here)
# (no runtime deps outside of Slicer itself)

# Testing
pytest>=7.4
pytest-cov>=4.1

# Code quality
black>=24.0
ruff>=0.4
mypy>=1.9

# Type stubs
types-setuptools
